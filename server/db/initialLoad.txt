CREATE TABLE partido (
   id SERIAL NOT NULL,
   fecha date NOT NULL,
   goles_blanco numeric not null,
   goles_azul numeric not null,
   CONSTRAINT id_partido_pk PRIMARY KEY (id)
);

CREATE TABLE jugador (
   id SERIAL NOT NULL,
   nombre VARCHAR(1000) NOT NULL,
   apellido VARCHAR(1000) NOT NULL,
   mail VARCHAR(1000) NOT NULL,
   telefono VARCHAR(1000),
   CONSTRAINT id_jugador_pk PRIMARY KEY (id)
);

CREATE TABLE partido_jugador (
   id_partido INTEGER NOT NULL REFERENCES partido(id),
   id_jugador INTEGER NOT NULL REFERENCES jugador(id),
   jugador_partido_id SERIAL NOT NULL,
   asistio boolean,
   condicion VARCHAR(20) not null
);

insert into jugador (
   nombre,
   apellido,
   mail,
   telefono
) values ('Javier', 'Gonzalez', 'javiermartingonzalez@gmail.com', '1150447225');

insert into jugador (
   nombre,
   apellido,
   mail,
   telefono
) values ('Daniel', 'Perez Lopez', 'danielplopez@gmail.com', '1150447225');

insert into partido_jugador (
   id_partido,
   id_jugador,
   asistio ,
   condicion) values (mmm,nnn, false, 'B')
);



